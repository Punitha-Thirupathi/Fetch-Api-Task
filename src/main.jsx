import React from "react";
import ReactDOM from "react-dom/client";
import Fetch from './FetchApi/Fetch.jsx' 


ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
  <Fetch /> 
  </React.StrictMode>
);
